namespace ARNavExperiment.Domain.Events
{
    /// <summary>
    /// Marker interface for all domain events.
    /// Domain events are readonly structs published via DomainEventBus.
    /// </summary>
    public interface IDomainEvent { }
}
